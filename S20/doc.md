# S20 — Academic Period Service (Сервис учебных периодов)

Сервис управляет семестрами, модулями и другими учебными периодами. Хранит даты начала и окончания каждого периода, его тип и привязку к учебным группам. **Не хранит** сами группы — они управляются Group Service.

---

## Добавить учебный период

Параметры, необходимые для создания учебного периода:

| Параметр         | Пояснение                        | Обязательность | Тип     | Ограничение                          | Значение по умолчанию |
|------------------|----------------------------------|----------------|---------|--------------------------------------|-----------------------|
| name             | Название периода                 | Да             | string  | 1–100 символов                       | —                     |
| academic_year_id | ID учебного года                 | Да             | integer | существующий id из AcademicYear      | —                     |
| period_type_id   | ID типа периода                  | Да             | integer | существующий id из PeriodType        | —                     |
| start_date       | Дата начала                      | Да             | string  | формат YYYY-MM-DD                    | —                     |
| end_date         | Дата окончания                   | Да             | string  | формат YYYY-MM-DD, позже start_date  | —                     |
| order_num        | Порядковый номер в учебном году  | Да             | integer | ≥ 1                                  | —                     |

**Уникальные комбинации параметров:**
- `(academic_year_id, period_type_id, order_num)` — в одном учебном году не может быть двух периодов одного типа с одинаковым порядковым номером.

Данные, возвращаемые при успешном создании:

| Параметр         | Тип               |
|------------------|-------------------|
| id               | integer           |
| name             | string            |
| academic_year_id | integer           |
| period_type_id   | integer           |
| start_date       | string (ISO 8601) |
| end_date         | string (ISO 8601) |
| order_num        | integer           |
| is_active        | boolean           |
| created_at       | string (ISO 8601) |
| updated_at       | string (ISO 8601) |

---

## Изменить учебный период по ID

Параметры для изменения (все необязательны):

| Параметр         | Пояснение                    | Обязательность | Тип     | Ограничение                         |
|------------------|------------------------------|----------------|---------|-------------------------------------|
| name             | Новое название               | Нет            | string  | 1–100 символов                      |
| academic_year_id | Новый учебный год            | Нет            | integer | существующий id                     |
| period_type_id   | Новый тип периода            | Нет            | integer | существующий id                     |
| start_date       | Новая дата начала            | Нет            | string  | формат YYYY-MM-DD                   |
| end_date         | Новая дата окончания         | Нет            | string  | формат YYYY-MM-DD, позже start_date |
| order_num        | Новый порядковый номер       | Нет            | integer | ≥ 1                                 |

Возвращаемые данные — полный объект периода (те же поля, что при создании):

| Параметр         | Тип               |
|------------------|-------------------|
| id               | integer           |
| name             | string            |
| academic_year_id | integer           |
| period_type_id   | integer           |
| start_date       | string (ISO 8601) |
| end_date         | string (ISO 8601) |
| order_num        | integer           |
| is_active        | boolean           |
| created_at       | string (ISO 8601) |
| updated_at       | string (ISO 8601) |

---

## Удалить учебный период по ID

Возвращает `true`, если период был деактивирован (`is_active = false`), иначе `false`. Физически запись из БД не удаляется.

---

## Получить учебный период по ID

| Параметр         | Пояснение               | Тип               |
|------------------|-------------------------|-------------------|
| id               | Идентификатор           | integer           |
| name             | Название                | string            |
| academic_year_id | ID учебного года        | integer           |
| period_type_id   | ID типа периода         | integer           |
| start_date       | Дата начала             | string (ISO 8601) |
| end_date         | Дата окончания          | string (ISO 8601) |
| order_num        | Порядковый номер        | integer           |
| is_active        | Активен ли период       | boolean           |
| created_at       | Дата создания           | string (ISO 8601) |
| updated_at       | Дата последнего изм.    | string (ISO 8601) |

---

## Получить список учебных периодов по параметрам

Параметры фильтрации:

| Параметр         | Пояснение                        | Тип     |
|------------------|----------------------------------|---------|
| academic_year_id | Фильтр по учебному году          | integer |
| period_type_id   | Фильтр по типу периода           | integer |
| is_active        | Фильтр по активности             | boolean |
| limit            | Кол-во записей (1–100)           | integer |
| offset           | Смещение (≥ 0)                   | integer |

Возвращается массив объектов периодов, каждый содержит:

| Параметр         | Тип               |
|------------------|-------------------|
| id               | integer           |
| name             | string            |
| academic_year_id | integer           |
| period_type_id   | integer           |
| start_date       | string (ISO 8601) |
| end_date         | string (ISO 8601) |
| order_num        | integer           |
| is_active        | boolean           |
| created_at       | string (ISO 8601) |
| updated_at       | string (ISO 8601) |

---

## Привязать учебную группу к периоду

| Параметр  | Пояснение         | Обязательность | Тип     | Ограничение             |
|-----------|-------------------|----------------|---------|-------------------------|
| period_id | ID периода        | Да             | integer | существующий id         |
| group_id  | ID учебной группы | Да             | integer | существующий id         |

Возвращаемые данные:

| Параметр  | Тип     |
|-----------|---------|
| id        | integer |
| period_id | integer |
| group_id  | integer |

---

## Отвязать учебную группу от периода по ID записи

Возвращает `true`, если запись удалена, иначе `false`.

---

## Получить список групп периода по ID периода

| Параметр  | Пояснение    | Тип     |
|-----------|--------------|---------|
| period_id | ID периода   | integer |

Возвращается массив объектов:

| Параметр  | Тип     |
|-----------|---------|
| id        | integer |
| period_id | integer |
| group_id  | integer |

---

## ER-диаграмма

> Файл диаграммы: `erd.drawio` (открывается в [draw.io](https://app.diagrams.net/))

![ERD](erd.drawio)

```
AcademicYear  ──< AcademicPeriod    (один учебный год — много периодов)
PeriodType    ──< AcademicPeriod    (один тип — много периодов)
AcademicPeriod >──< Group  через PeriodGroup  (многие ко многим)
```

| Таблица         | Назначение                                                   |
|-----------------|--------------------------------------------------------------|
| AcademicYear    | Учебный год (напр. 2024-2025)                                |
| PeriodType      | Справочник типов: Семестр, Модуль и т.д.                     |
| AcademicPeriod  | Основная сущность: конкретный период с датами                |
| PeriodGroup     | Транзитивная таблица: привязка периодов к учебным группам    |
